import stringChange from "./string";
import NumberChange from "./number";
import timeChange from "./time";

const lbr = {
  string: stringChange,
  number: NumberChange,
  time: timeChange,
};

export default lbr;
